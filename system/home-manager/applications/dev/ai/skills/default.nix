{
  # Custom skills shared across AI coding agent CLIs (Claude Code, Pi).
  # Each is a directory containing a SKILL.md following the Agent Skills
  # spec (name + description frontmatter), so the same directory can be
  # dropped into any agent that understands that spec.
  names = [
    "error-handling"
    "git-commit"
    "todo-comments"
    "wayfinder"
  ];
}
